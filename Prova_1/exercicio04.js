let numeros = [5, 2, 3, 2, 4];
let soma = 0; //aux ou soma

for (let i = 0; i < numeros.length; i++) {
  console.log(soma);
  console.log("Numero", numeros[i]);
  //soma = soma + numeros[i]
  soma += numeros[i];
}

console.log(soma);

//1 ponto.
